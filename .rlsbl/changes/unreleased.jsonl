{"format_version":1,"id":"18d8527c78fcaa21223cd71804294ea8a9cb1b923873c41e","commits":["4db9de47f1c358b95442aa948e2722ebbe8a54c4"],"user_facing":false}
{"format_version":1,"id":"18d8528506748017814690268c0c4ac584f08043e6f0498d","commits":["3a51f53a66f8eff01403e1873e92e58962bcaa8f"],"user_facing":true,"description":"**Scaffolded Go-installing CI reads the Go version from a file.** The pgdesign target's CI installs the Go declared in `.go-version` next to `pgdesign.toml` instead of the floating `stable`; create that file (one line, e.g. `1.26.6`) and re-run `rlsbl scaffold`. The Go target's CI no longer renders a `# go.mod: go X` comment restating the floor, and explains that `setup-go` reads go.mod's `toolchain` line first: declare the development Go there (`go mod edit -toolchain=go1.26.6`) so CI tests with it while `go` stays the consumers' floor.","type":"fix"}
{"format_version":1,"id":"18d8528517a1e688b2ab7884c2044c91802b503d6ecffda3","commits":["5c1c0b625e1263a7bc5d2d21dc5d795fa52cc99d"],"user_facing":false}
{"format_version":1,"id":"18d857b5bde9e26488b4d1b2de1d4141829f1c93eeb4124d","commits":["c2fbd50d6c8b1c5b3d4188a11468dec3fb11cee7"],"user_facing":true,"description":"**`rlsbl rewrite go-module-path` no longer crashes on deeply nested generated code, and no source walk reads gitignored files.** A generated file whose expression nests thousands of levels deep (a protobuf descriptor built as one long string concatenation) crashed the rewrite with a bare RecursionError; every syntax-tree walker in the Go, npm, and Python linters now handles any depth, and a file that cannot be analyzed is named in the error. The rewrite, the linters, and the dependency and ldflags checks now consider exactly what `git ls-files --cached --others --exclude-standard` lists: tracked files plus untracked files that are not ignored, so a gitignored third-party clone is never touched, and a directory outside any git work tree is refused.","type":"fix"}
{"format_version":1,"id":"18d85805c6e92dc384f39830006b4db4bf17b8353a7885be","commits":["1cd0d539ab44f641bed21041323bb1694b336f8c"],"user_facing":true,"description":"**Scaffolding a releasable monorepo member no longer leaves release state the `releasable-residue` check refuses.** The member's three-way merge bases are kept in its releasable's state directory (`.rlsbl-monorepo/releasables/<name>/bases/<member path>/`) instead of the member's `.rlsbl/bases/`, and no `.rlsbl/version` marker is written into the member. A member scaffolded before this has a `managed-files.json` but no bases at the new location, so the next scaffold refuses and names the one-time heal (create that directory and re-run: each base is reconstructed from the last scaffold commit); `rlsbl monorepo cleanup` then removes the old per-member copies.","type":"fix"}
{"format_version":1,"id":"18d85928707f2aa9c9c7113d334344a1bb51a2f4c467814d","commits":["bd15ae81841501acfe0628cf09313ae13e5d6f24"],"user_facing":true,"description":"**The scaffolded npm CI tests exactly the Node lines `engines.node` admits, and a package without `engines.node` is refused.** The `node-version` matrix of an npm project's `ci.yml` (npm, pnpm, and yarn alike) used to be a fixed `[20, 22, 24]`; it is now every supported Node line (20, 22, 24) whose newest release satisfies `package.json`'s `engines.node` range (`>=22` gives `[22, 24]`). `rlsbl scaffold` refuses a package that declares no `engines.node`, naming the declaration to add (e.g. `\"engines\": {\"node\": \">=22\"}`), and refuses a range it cannot read or one that admits none of those lines.","type":"breaking"}
{"format_version":1,"id":"18d85935ce12268ccd121dac51a54b9380fb25489b5e366e","commits":["d07b41b11449e4cb378ddbbd16b6c7c420a1b969"],"user_facing":true,"description":"**The `[tool.pytest.ini_options]` table scaffold adds to `pyproject.toml` is separated from the next table by a blank line.** The next table header used to follow on the line right after `norecursedirs`.","type":"fix"}
{"format_version":1,"id":"18d8597bb21a1cdc6f0697be6e824afca868a983c7df3fc8","commits":["d70674a72bcbe4e98d2bd5de48763affeddd9e5d"],"user_facing":true,"description":"**Scaffold names why it removes an existing `publish.yml`.** A project that gets no publish workflow (a monorepo member declared `releasable = false`, a project with `publish_mode: \"none\"`, or a workspace root) has an earlier scaffold's `publish.yml` removed on re-scaffold; the file report now says why (e.g. `removed (this member releases nothing (releasable = false), so it gets no publish workflow)`) instead of a bare `removed (orphan)`.","type":"fix"}
{"format_version":1,"id":"18d859da13e28f8ad777689c76114015bf32387d64408ffb","commits":["3ddd1886a4f07c148dca740265f75e4e706edc6d"],"user_facing":true,"description":"**An npm target whose directory has no `package.json` is refused with the workspace member and the missing file named.** `rlsbl scaffold` crashed with a `FileNotFoundError` traceback, and `rlsbl monorepo sync` printed one per member and generated the router anyway; both now stop with e.g. `workspace member 'protocols' (protocols/) has an npm target, but protocols/npm/package.json does not exist`, naming the two ways out (create the package, or remove the npm entry from the targets list that declares it, which for a releasable member is the releasable's `config.json`).","type":"fix"}
{"format_version":1,"id":"18d85a035b089fc0b345f50e0b12441fac12eb38544b4c34","commits":["43438980fd1b9105f985f8effe9a5154262bf6a7"],"user_facing":true,"description":"**`rlsbl scaffold` refuses while `experiments/` or `screenshots/` holds tracked files.** Scaffold makes git ignore everything in those scratch directories, so files already committed there would stay tracked while every new file beside them is silently ignored. The refusal lists each tracked file (the scratch `.gitignore` and a Go project's `go.mod` there are scaffold's own and never count) and the remedies: move them to a committed home, e.g. `mkdir -p assets && git mv screenshots/hero.png assets/hero.png`, or rename the directory if it is not scratch space; commit the move and re-run.","type":"breaking"}
{"format_version":1,"id":"18d85a6484c72be9ae434c692c114e57a4947ceb6c48c8bc","commits":["6295b14b6b8ee5267b27d3b2480df498bb556358"],"user_facing":true,"description":"**New release-blocking check `go-toolchain-declared`: a Go target's `go.mod` must carry a `toolchain` line.** `actions/setup-go` installs the Go that line names and, without it, the `go` directive's version (the oldest Go consumers may build with), so CI was testing on a Go nobody develops with. The check (tags `project` and `preflight`, so it blocks a release) asks only whether the line is present and never compares it with the Go installed on the machine; each finding names the remedy, `go mod edit -toolchain=<version>` in the module's directory (e.g. `-toolchain=go1.26.6`).","type":"breaking"}
{"format_version":1,"id":"18d85a6494588b04bfe056e295d8428782906394d7126959","commits":["0ea8bf21512057d396330e7b8242838a482b5c60"],"user_facing":true,"description":"**The pgdesign target's scaffold creates `.go-version` beside `pgdesign.toml` when it is missing.** Its CI installs the Go that file names; scaffold seeds it from the Go on this machine (`go env GOVERSION`), never rewrites an existing one (change the version by editing the file), and when there is no go command refuses, naming the file and the one line to write.","type":"feature"}
{"format_version":1,"id":"18d85a92721463e0fdeff9ada0e0401082d36563d6d157a4","commits":["213d8b967c9c7f3113ea9908ff53ad3c29403984"],"user_facing":true,"description":"**`rlsbl claim-name` uses the registry's own login when no token is set.** npm claims use `NPM_TOKEN` when it is set and otherwise npm's own `~/.npmrc` login (`npm login`); PyPI claims use `UV_PUBLISH_TOKEN` or `PYPI_TOKEN` when set and otherwise the token in `~/.pypirc`'s `[pypi]` section. A claim is refused only when neither source exists, and the refusal names both; no token is ever printed.","type":"feature"}
